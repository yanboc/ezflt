import seaborn

def visualize(feature, colored=True, output_size=[1,0]):
    """

    Args:
        feature (torch.tensor): the feature to be visualized. 
        colored (boolean):
        output_size (list):  
    """

    